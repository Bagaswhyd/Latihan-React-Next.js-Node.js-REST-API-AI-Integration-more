Practice Passing Props & Manipulations
> Objective_1: Display beautiful cards with singer information (name, when born, instruments, genres and partners).

App.js

1 ) Pass singer prop to the Card component
2 ) Pass background prop with ivory value to the first Card component
3 ) Pass background prop with wheat value to the third Card component

Card.js

1 ) Set default value as lightgray for the background prop in the Card.js component

> Objective_2: Display beautiful cards with singer information (name, when born, instruments, genres and partners).

App.js

1 ) Pass singer prop to the Card component
— The syntax for passing props to the React component is similar to the HTML attributes syntax.
2 ) Pass background prop with ivory value to the first Card component
— The syntax for passing props to the React component is similar to the HTML attributes syntax.
3 ) Pass background prop with wheat value to the third Card component
— The syntax for passing props to the React component is similar to the HTML attributes syntax.
Card.js
1 ) Set default value as lightgray for the background prop in the Card.js component
— Read React component props using function parameters